sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t igorgroz/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true igorgroz/react-test npm test

after_success:
  - docker build -t igorgroz/multi-client ./client
  - docker build -t igorgroz/multi-nginx ./nginx
  - docker build -t igorgroz/multi-server ./server
  - docker build -t igorgroz/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push igorgroz/multi-client
  - docker push igorgroz/multi-nginx
  - docker push igorgroz/multi-server
  - docker push igorgroz/multi-worker